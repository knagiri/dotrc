# CLAUDE.md（dotrc repo）

この repo の checkout（linked worktree を含む）でだけ効かせる規約。`dot/claude/` 配下は
`~/.claude/` へ展開されて他 repo の session にも配られるので、この repo 固有の規約はそちらへ
置かず、ここに置く。

## 勤務先由来の具体名を書かない

この repo は public である。tracked ファイル・issue 本文・PR 本文・commit message には、
勤務先由来の具体名を一般に書かない。対象は組織名・repo 名・ブランチ名・サービス名・拠点名・
勤務先 repo の PR 番号・勤務先のメールアドレス。ファイル種別は問わず、rule・skill・agent 定義・
design doc・テスト fixture・スクリプトのコメントも同じ扱いになる。

- **実例は機構で書く。** 「何がどう黙って壊れるか」を書けば規約として足りる。どの repo で
  起きたかは規約の適用の助けにならない。rule ファイルの由来の割り方は
  `dot/claude/rules/rule-authoring.md` の「由来は機構と記録に割り、記録はポインタだけ置く」節に
  従う（この節はその適用範囲をファイル種別を問わず広げるもの）
- **値が要るなら架空データにする。** サンプル出力やテスト fixture は、誰が見ても架空と分かる
  値（`example-org` / `example-repo` / `feature/foo` 等）にする。実物を写して一部だけ伏せない。
  伏せ漏れが残りやすく、残った断片から元が辿れる
- **勤務先の環境で観測したものを持ち込むときが最も危ない。** ログ・コマンド出力・session 名・
  worktree のパスをそのまま貼らず、機構だけを写す。観測結果は具体名ごと出力されるので、
  貼った時点で混入する

なぜ: 実例として自然に書いた記述が、issue 本文・design doc のサンプル出力・skill の実例・
テスト fixture に混入して公開され、回収に履歴の書き換えが要った。書いている最中は「具体例を
添えて分かりやすくした」としか見えず気づけないので、書き方の側で縛る。一度 push された具体名は
revert しても履歴と fork・キャッシュに残る。
